# 安全说明

## 安全基线

Python 依赖以带哈希的 `requirements.lock` 固定；CI 在提交、PR 和每周定时运行依赖漏洞审计，不忽略公告或允许审计失败。审计结果只代表公告库快照，需持续复核。

复核命令（需先安装 pip-audit）：

```bash
python -m pip_audit -r requirements.lock --no-deps --disable-pip
npm audit --prefix web --registry=https://registry.npmjs.org
```

Python 命令审计锁文件列出的固定包，不审计操作系统包、容器基础镜像或未列入该锁文件的开发工具。

兼容性：Paramiko 5 移除了 RSA SHA-1 签名、SHA-1 密钥交换与 GSSAPI，并提高 DH group-exchange 的最低模数要求。仅支持旧算法的 SSH/SFTP/Git SSH 服务器需要升级配置，不能通过恢复 SHA-1 绕过。RSA 密钥仍可通过 `rsa-sha2-256/512` 使用，参见 [Paramiko 5 发布说明](https://www.paramiko.org/changelog.html)。

### Git 适配器

- 克隆、拉取和健康检查每次联网均重新校验 URL、出站登记与全部 DNS 结果。HTTP(S) 使用 Git 原生 `http.curloptResolve` 固定 IP，保留原始 Host/TLS SNI 和证书主机名验证；禁止重定向、HTTP alternates、环境代理和非预期协议。
- 隔离全局 Git 配置，联网命令在独立临时目录运行并禁止向父目录发现仓库，避免健康检查继承父仓库的 URL rewrite；拉取前严格检查缓存 `.git/config`，拒绝 remote 改写、URL rewrite、include、代理和外部 credential helper 等配置。实际拉取显式使用已校验的资产 URL，配置不一致时返回 `REPO_CONFIG_UNSAFE`。
- SSH 先验证主机密钥再写私钥；每次操作使用独立 0700 临时目录，私钥、known_hosts 和 SSH 配置创建即为 0600。准备失败或操作退出均清理，并发健康检查不会覆盖或删除另一操作的凭据。固定 IP 后通过 HostKeyAlias 对应已验证的 known_hosts。
- Git 同步/健康检查的原始 stderr 不进入返回错误或同步错误日志；Token 仅放在隔离的子进程环境，不落盘或进入命令参数。此改动不等于所有适配器和业务日志已全面脱敏。

Git HTTP(S) 要求运行环境支持 `http.curloptResolve`；不支持时返回 `GIT_UNSUPPORTED`，不会回退到未经固定的 DNS 连接。子进程优先使用系统 `/usr/bin`、`/bin` 工具，不继承用户 PATH；Docker 构建新增 Git 能力和依赖一致性检查。仓库 URL 必须直接指向最终地址，不支持依赖重定向、代理或全局 Git 配置的接入方式。

### 验证范围

- 默认测试不访问真实资产；远端契约、全栈 e2e、真实浏览器与稳定性压测需显式配置才会执行，未执行时不得计为通过。
- 生产部署前应在干净环境完成部署演练、迁移与备份恢复验证；验证结论只覆盖实际执行的范围。

## 运行数据与对外分享

- 凭据加密不等于数据库可以公开：资产地址、名称、账号配置、授权与参数规则、访问审计可能包含组织信息，数据库和备份都属于私有数据。
- 网关会把授权工具的业务结果返回调用客户端，可能继续传给外部模型服务商。管理员负责确认数据可外传范围，最小化账号权限；工具参数规则不脱敏查询结果。
- 日志、代码仓库副本、构建日志、截图和导出文件也可能含业务秘密；不保证所有上游内容或异常栈都已脱敏。
- `.gitignore` 只控制 Git 候选，不控制手动压缩包、截图、远程打包、镜像层或第三方云服务。

## 漏洞反馈

请勿通过公开 Issue 提交可利用细节、真实凭据、生产数据库或完整配置。仓库开启 GitHub 私密漏洞报告后，请使用 Security → Report a vulnerability；若尚未开启，可先发不包含敏感信息的 Issue 请求维护者提供私密联系渠道。未承诺固定响应时限。

报告请包含受影响版本、最小复现、影响范围和脱敏日志。令牌、会话 cookie、数据库备份、主密钥及账号凭据不得附入报告。

## 部署边界

- 管理员拥有资产、账号和授权配置能力；不要把管理员凭据交给不可信客户端。
- 网关的 SQL、命令及路径检查是纵深防御，不能替代远端只读账号、SSH 命令限制、SFTP chroot、网络防火墙。
- MCP 上游工具可以有外部副作用。批准工具定义版本不表示工具安全，需要信任上游并按最小权限授权。
- 工具授权跨启用组取并集；参数白名单按完整组条件取并集，任一适用组的参数黑名单优先拒绝。禁用/删除含黑名单的组可能放宽其他授权；彻底撤权应检查全部关联组，或禁用对应客户端/账号。
- SSH 命令不再受内置固定程序或账号 `command_allowlist` 限制，改由授权组 `cmd` 参数规则控制。未配置规则时不额外限制；黑名单并非 shell 沙箱，不能防止所有等价命令、编码或子进程绕过，建议精确白名单与远端最小权限。
- Kubernetes `k8s_exec` 为有副作用的非交互工具，必须显式授权，且受命名空间、`pods` 类型、集群 RBAC 和参数规则限制；禁止 stdin/TTY，不自动重试。截断、异常断流或超时不证明远端进程已终止。
- Jenkins 直接使用用户名/API Token 调用 HTTP API，凭据加密托管；HTTPS 使用独立 SSLContext 校验证书，URL 禁止内嵌凭据，所有请求固定来源/IP。仅已知写操作的同来源精确目标 302/303 可被接收，仍不跟随重定向；Cookie 按原始逻辑来源维护，crumb 不通过重复 POST 试错。
- Jenkins 的触发、更新构建、重建、Replay 均需显式授权；参数规则仅限制入参，不过滤查询结果。项目隔离依赖 Jenkins 账号权限，Replay 能执行 Pipeline 代码，必须同时评估 Jenkins 的 Replay/构建/配置读取及脚本审批权限；不开放 `/scriptText`、任务配置写入或管理操作。
- Jenkins 原始 config.xml 不对外输出，XML 禁止 DTD/实体；Replay 仅结构化解析已知表单，不执行页面脚本，未完整获取源码时拒绝提交。日志和源码可能包含业务秘密，Jenkins 端必须做好掩码；审计只存参数名与摘要，托管 Token 不进入结果。签名日志游标绑定资产/账号/任务/构建，不代表授权，续读仍须经过实时授权检查。
- 参数正则采用全文匹配，最长 4096 字符，运行期总匹配预算 100ms，超时拒绝。规则仅支持顶层参数；只信任管理员编写的规则，避免将不可信文本作为正则配置。
- 生产环境应配置非空 `OUTBOUND_ALLOWLIST`（登记列表非空即启用强制登记），精确登记目标主机和端口；`null` 表示不启用登记校验，仅可在完全可信的网络中临时使用。生产使用 HTTPS、Secure cookie 和严格的来源/主机限制。
- SSH/SFTP 主机指纹校验默认关闭（`SSH_HOST_KEY_ENFORCE=false`，接受任意主机密钥）；建议生产设为 `true`，使指纹字段回到创建/编辑表单并在连接时强制校验。
- 加密主密钥与数据库分开存储和备份。备份包含敏感数据，即使凭据已加密也不可公开。
- 工具超时可能意味着远端状态未知；不要自动重试具有副作用的调用。
- 当前仅支持单实例 SQLite；不支持共享数据库多进程部署。

## 发布安全

公开前检查候选文件、暂存内容和全部 Git 历史。`.gitignore` 不会移除已经跟踪的文件；`scripts/release_check.py` 只是常见模式检查，不是完整秘密扫描。发现历史泄露时首先轮换凭据，再处理历史，不要只删除最新版本的文件。

发布者应开启可用的 secret scanning / push protection，定期运行依赖漏洞审计并处理 Dependabot 提示。MIT 许可不包含安全性或适用性保证。
